version: '2'
services:
    myretailproducts-mongodb:
        container_name: myretailproducts-mongodb
        image: mongo:3.3.12
        ports:
            - "27017:27017"
        command: mongos --configdb myretailproducts-mongodb-config
    myretailproducts-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    myretailproducts-mongodb-config:
        image: mongo:mongo:3.3.12
        container_name: myretailproducts-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
